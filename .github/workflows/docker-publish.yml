name: Docker Image Build & Publish

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
    inputs:
      reason:
        description: 'Manuel çalıştırma sebebi'
        required: false
        default: 'Manuel tetikleme'

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # --- GARANTİ DÜZELTME ---
      # Repo adını küçültüp bir 'output' (çıktı) olarak saklıyoruz.
      # Bu yöntem env değişkeninden daha güvenilirdir.
      - name: Lowercase Image Name
        id: lowercase_repo
        run: |
          LOWERCASE_NAME=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          echo "image_name=$LOWERCASE_NAME" >> $GITHUB_OUTPUT
          echo "Dönüştürülen isim: $LOWERCASE_NAME"

      - name: Determine Next Version Tag
        id: next_version
        run: |
          LATEST_TAG=$(git describe --tags --match "v*" --abbrev=0 2>/dev/null || echo "v0.0.0")
          
          if [ "$LATEST_TAG" == "v0.0.0" ]; then
            NEW_VERSION="v0.0.1"
          else
            MAJOR=$(echo $LATEST_TAG | sed 's/v//' | cut -d'.' -f1)
            MINOR=$(echo $LATEST_TAG | sed 's/v//' | cut -d'.' -f2)
            PATCH=$(echo $LATEST_TAG | sed 's/v//' | cut -d'.' -f3)
            
            if [ -z "$PATCH" ]; then PATCH=0; fi

            NEW_PATCH=$((PATCH + 1))
            NEW_VERSION="v${MAJOR}.${MINOR}.${NEW_PATCH}"
          fi
          
          echo "Calculated New Version: $NEW_VERSION"
          echo "NEW_VERSION=$NEW_VERSION" >> $GITHUB_OUTPUT

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          # BURASI DEĞİŞTİ: Artık doğrudan yukarıdaki adımın çıktısını (outputs) kullanıyoruz.
          # Bu sayede büyük harf kalma ihtimali %0'dır.
          tags: |
            ${{ env.REGISTRY }}/${{ steps.lowercase_repo.outputs.image_name }}:${{ steps.next_version.outputs.NEW_VERSION }}
            ${{ env.REGISTRY }}/${{ steps.lowercase_repo.outputs.image_name }}:latest

      - name: Create Git Tag
        run: git tag ${{ steps.next_version.outputs.NEW_VERSION }}

      - name: Push Git Tag
        run: git push origin ${{ steps.next_version.outputs.NEW_VERSION }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.next_version.outputs.NEW_VERSION }}
          name: Release ${{ steps.next_version.outputs.NEW_VERSION }}
          # Release metninde de düzeltilmiş ismi kullanıyoruz
          body: |
            Bu sürüm otomatik oluşturulmuştur.
            
            **Docker İmajı:** `docker pull ${{ env.REGISTRY }}/${{ steps.lowercase_repo.outputs.image_name }}:${{ steps.next_version.outputs.NEW_VERSION }}`
          draft: false
          prerelease: false
